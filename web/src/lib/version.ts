// Product version string, surfaced in the footer (ADR-0016 §2).
// Display form X.XX.XXX (ADR-0068); web/package.json holds the semver form.
// Bump both together on each release.
export const VERSION = "0.34.001";
